{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "Cap helm/chart-releaser-action.",
      "matchDatasources": ["github-tags"],
      "matchPackageNames": ["helm/chart-releaser-action"],
      "allowedVersions": "<=1.5.0"
    },
    {
      "description": "Keep .nvmrc on a range rather than pinning it, so repos stay on the Node line they target",
      "matchManagers": ["nvm"],
      "rangeStrategy": "replace"
    },
    {
      "description": "Runner version bumps break workflows and are easy to merge by mistake, so BRE updates them deliberately.",
      "matchDatasources": ["github-runners"],
      "enabled": false
    }
  ]
}
